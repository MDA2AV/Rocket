<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net11.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>Playground.Shared</RootNamespace>
  </PropertyGroup>

  <!-- Deliberately NO reference to ioxide. Nothing in this project touches an ioxide API: it is
       env-var parsing, self-signed cert generation and seeding a demo directory - plumbing that
       would otherwise bury the part of each sample worth reading. Everything that shows how to USE
       ioxide lives inline in the sample, so a Program.cs can be copied out whole. -->

</Project>
